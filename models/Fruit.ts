export default class Fruit {
  type: string;
  weight: number;
  color: string;

  constructor(type: string, weight: number, color: string) {
    this.type = type;
    this.weight = weight;
    this.color = color;
  }
}